import { ScrollContainer, ScrollLine, ScrollText } from "./ScrollDownLine.style"

export const ScrollDownLine = () => {
  return (
    <ScrollContainer>
        <ScrollText>Scroll</ScrollText>
        <ScrollLine />
    </ScrollContainer>
  )
}
